import { ShowImagePipe } from './show-image.pipe';

describe('ShowImagePipe', () => {
  it('create an instance', () => {
    const pipe = new ShowImagePipe();
    expect(pipe).toBeTruthy();
  });
});
